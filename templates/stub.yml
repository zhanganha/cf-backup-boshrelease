---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: cf_backup1
  security_groups:
    - cf_backup

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: cf-backup
    version: latest
